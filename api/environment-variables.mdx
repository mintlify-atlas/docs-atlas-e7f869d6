---
title: 'Environment Variables'
description: 'Configuration and secrets management for the XAUUSD Trading Bot'
---

## Overview

The XAUUSD Trading Bot requires API credentials to function. This page covers the required environment variables and best practices for managing them securely.

## Required Variables

### GROQ_API_KEY

The GROQ API key is used to power the LLM-based market analysis and signal generation.

<ParamField path="GROQ_API_KEY" type="string" required>
  Your GROQ API key for accessing LLM services. This key must be kept secure and never committed to version control.
</ParamField>

## Configuration Methods

### Streamlit Secrets

When deploying with Streamlit, use the built-in secrets management:

<CodeGroup>

```toml .streamlit/secrets.toml
# Local development secrets file
# Add this file to .gitignore

GROQ_API_KEY = "your-groq-api-key-here"
```

```python app.py
import streamlit as st
from XAUSD_AI import XAUUSDTradingBot

# Access API key from secrets
bot = XAUUSDTradingBot(api_key=st.secrets["GROQ_API_KEY"])
```

```bash Streamlit Cloud
# For Streamlit Cloud deployment:
# 1. Go to your app settings
# 2. Navigate to "Secrets" section
# 3. Add your secrets in TOML format:

GROQ_API_KEY = "your-groq-api-key-here"
```

</CodeGroup>

### Environment Variables

For non-Streamlit deployments, use standard environment variables:

<CodeGroup>

```bash Unix/Linux/macOS
# Set environment variable
export GROQ_API_KEY="your-groq-api-key-here"

# Run your application
python your_app.py
```

```bash Windows (Command Prompt)
# Set environment variable
set GROQ_API_KEY=your-groq-api-key-here

# Run your application
python your_app.py
```

```bash Windows (PowerShell)
# Set environment variable
$env:GROQ_API_KEY="your-groq-api-key-here"

# Run your application
python your_app.py
```

```python Python Usage
import os
from XAUSD_AI import XAUUSDTradingBot

# Load API key from environment
api_key = os.getenv("GROQ_API_KEY")

if not api_key:
    raise ValueError("GROQ_API_KEY environment variable is not set")

bot = XAUUSDTradingBot(api_key=api_key)
```

</CodeGroup>

### .env File

For local development, use a `.env` file with python-dotenv:

<CodeGroup>

```bash .env
# Add this file to .gitignore
GROQ_API_KEY=your-groq-api-key-here
```

```python app.py
import os
from dotenv import load_dotenv
from XAUSD_AI import XAUUSDTradingBot

# Load environment variables from .env file
load_dotenv()

# Initialize bot with API key
bot = XAUUSDTradingBot(api_key=os.getenv("GROQ_API_KEY"))
```

```bash Installation
# Install python-dotenv
pip install python-dotenv
```

</CodeGroup>

## Security Best Practices

<Expandable title="Never Commit Secrets">
  Always add secrets files to `.gitignore` to prevent accidental commits:
  
  ```bash .gitignore
  # Secrets and environment files
  .env
  .streamlit/secrets.toml
  *.key
  credentials.json
  ```
</Expandable>

<Expandable title="Use Secrets Management Services">
  For production deployments, consider using dedicated secrets management:
  
  - **Streamlit Cloud**: Built-in secrets management in app settings
  - **AWS**: AWS Secrets Manager or Parameter Store
  - **Google Cloud**: Secret Manager
  - **Azure**: Key Vault
  - **HashiCorp**: Vault
</Expandable>

<Expandable title="Rotate Keys Regularly">
  - Change API keys periodically (e.g., every 90 days)
  - Immediately rotate keys if you suspect they've been compromised
  - Keep track of key rotation dates
</Expandable>

<Expandable title="Limit Key Permissions">
  - Use API keys with minimum required permissions
  - Create separate keys for development and production
  - Monitor API key usage for unusual activity
</Expandable>

<Expandable title="Validate Before Use">
  Always validate that required environment variables are set:
  
  ```python
  import os
  
  def get_api_key():
      api_key = os.getenv("GROQ_API_KEY")
      if not api_key:
          raise ValueError(
              "GROQ_API_KEY is not set. "
              "Please configure it in your environment or secrets."
          )
      return api_key
  
  bot = XAUUSDTradingBot(api_key=get_api_key())
  ```
</Expandable>

## Obtaining a GROQ API Key

1. Visit the [GROQ Console](https://console.groq.com)
2. Sign up or log in to your account
3. Navigate to API Keys section
4. Generate a new API key
5. Copy and securely store your key
6. Configure it in your application using one of the methods above

## Troubleshooting

### Key Not Found Error

```python
# Error: KeyError: 'GROQ_API_KEY'
# Solution: Verify the key is set

import streamlit as st

if "GROQ_API_KEY" not in st.secrets:
    st.error("GROQ_API_KEY not found in secrets. Please configure it.")
    st.stop()

bot = XAUUSDTradingBot(api_key=st.secrets["GROQ_API_KEY"])
```

### Invalid API Key

```python
# Error: Authentication failed
# Solution: Verify key is correct and active

try:
    bot = XAUUSDTradingBot(api_key=st.secrets["GROQ_API_KEY"])
    result = bot.run_analysis()
except Exception as e:
    if "authentication" in str(e).lower():
        st.error("Invalid API key. Please check your GROQ_API_KEY.")
    else:
        st.error(f"Error: {str(e)}")
```

### Secrets File Not Loading

```bash
# Streamlit secrets location:
# .streamlit/secrets.toml (local)

# Verify file exists and has correct format
cat .streamlit/secrets.toml

# Ensure proper TOML syntax (no quotes around key names)
GROQ_API_KEY = "your-key-here"
```

## Example: Complete Setup

<CodeGroup>

```toml Step 1: Create secrets file
# .streamlit/secrets.toml
GROQ_API_KEY = "gsk_your_actual_groq_api_key_here"
```

```bash Step 2: Add to .gitignore
# .gitignore
.streamlit/secrets.toml
.env
*.key
```

```python Step 3: Use in application
import streamlit as st
from XAUSD_AI import XAUUSDTradingBot

# Validate key exists
if "GROQ_API_KEY" not in st.secrets:
    st.error("Please configure GROQ_API_KEY in .streamlit/secrets.toml")
    st.stop()

# Initialize bot
bot = XAUUSDTradingBot(api_key=st.secrets["GROQ_API_KEY"])

# Use bot
result = bot.run_analysis()
```

</CodeGroup>